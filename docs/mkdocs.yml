# This file exists to satisfy the repository layout in AGENTS.md §1.
# MkDocs reads the configuration from the repo root mkdocs.yml.
# The authoritative configuration is at: ../mkdocs.yml
#
# DO NOT add configuration here — edit ../mkdocs.yml instead.
